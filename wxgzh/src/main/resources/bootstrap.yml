eureka:
  client:
    service-url:
      defaultZone: http://admin:admin@localhost:9091/eureka/
  instance:
    instance-id: ${spring.application.name}:${spring.cloud.client.ip_address}:${server.port}